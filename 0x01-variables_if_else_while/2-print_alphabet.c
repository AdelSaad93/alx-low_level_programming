#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print the alphabet in lowercase,
 *
 * Return: always 0
 */
int main(void)
{
	char ch = 'a';

	while
	{
		putchar(ch);
		ch++;
	}
	putcha('\n');

	return (0);
}
